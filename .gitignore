# dependencies
node_modules/

# build output
dist/

# env & secrets
.env
.env.local

# local registry / mapping / authorization (machine-specific, ignored)
projects.json
usermap.json
bugfix-allowlist.json
bugfix-allowed-departments.json
code-read-allowlist.json
code-read-allowed-chats.json

# session persistence (SQLite); WAL/SHM sidecar files included
session.db
session.db-wal
session.db-shm

# logs
*.log

# docker-compose runtime volumes (see docs/deployment.md)
/data/
/worktrees/
/claude-home/

# OS / editor
.DS_Store
Thumbs.db
