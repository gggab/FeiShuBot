node_modules
dist

.env
.env.local

# machine-specific config (mounted as volumes at runtime, not baked into the image)
projects.json
usermap.json
bugfix-allowlist.json
bugfix-allowed-departments.json
code-read-allowlist.json
code-read-allowed-chats.json

session.db
session.db-wal
session.db-shm
*.log

.git
tests
docs
*.md
# IDENTITY.md is COPYed into the image (Dockerfile) as the baked-in default; keep it in context.
!IDENTITY.md

data
worktrees
claude-home
